<h1>Viewing Restuarant profile in show</h1>

<h2>The restaurant id is <%= restaurant.id %></h2>
<% if(locals.isLoggedIn){ %>
  <h2>The user  id is <%= locals.users.id %> %></h2>
  <h2>The user is logged in is <%= locals.isLoggedIn %> %></h2>
  <% } %>

<h2>Locals is <%= locals.restaurant %> %></h2>


<h2><%= restaurant.restaurantName %></h2>
<p><%= restaurant.address %></p>
<p><%= restaurant.city %></p>
<p><%= restaurant.cuisine %></p>
<% restaurant.comments.forEach(comment => { %>
    <li><%= comment %></li>
  <% }) %>
<hr>
<form class="" action="/restaurant/<%= restaurant._id %>" method="POST">
  <input type="text" name="comments" id="comments" value="">
  <label for="comments">comments</label>
  <%# <input type="hidden" name="userId" value="<%= locals.users.id %>"> %>
  <button>Submit</button>
</form>
<hr>

<a href='/restaurant/<%= restaurant._id %>/edit'>Edit Restaurant</a>

</div>
