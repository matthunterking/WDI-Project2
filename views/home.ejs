<% if(locals.isLoggedIn){ %>
  <h1>Welcome <%= locals.user.name %></h1>
  <h1>Locals user id is <%= locals.user.id %></h1>
  <h1>Locals is <%= locals.user %></h1>
  <a href='/logout'>Logout</a>
  <a href='/users/<%= user._id %>'>My profile</a>
<% } else { %>
  <a href='/signin'>Login</a>
  or
  <a href='/users/new'>Sign up</a>
  <a href='/restaurant/new'>Add a Restaurant</a>
  <% } %>
<h1>Homepage</h1>

<h2>Restuarants</h2>
<% restaurants.forEach(restaurant => { %>
  <p><%= restaurant.restaurantName %></p>
  <p><%= restaurant.address %></p>
  <p><%= restaurant.city %></p>
  <p><%= restaurant.cuisine %></p>
  <p><%= restaurant.comments %></p>
  <a href="/restaurant/<%= restaurant._id %>">View Restuarant</a>
<% }) %>
